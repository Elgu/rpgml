
print( [ "a", "b", "c" ][ 1 ] + "\n" );

#int[][] a = [ [ 0 to 0 ], [ 0 to 1 ], [ 0 to 2 ], [ 0 to 4 ], [ 0 to 5 ] ];
#
#for i in a
#{
#  for j in i
#  {
#    print( j );
#  }
#  print( "\n" );
#}

int[2,3] c = 1;

c[1,1] = 2;

for i = 0 to 1
{
  for j = 0 to 2
  {
    print( c[i,j] );
  }
  print( "\n" );
}

Function F( x )
{
  return { int i=x; };
}

F f( 23 );
print( f.i + "\n" );

{ int i=42; }
F( 11 );

Function A( x )
{
  int i = x;
  int j = 2*x;
  return this;
}

A a( 42 );
print( a.i + ", " + a.j + "\n" );


